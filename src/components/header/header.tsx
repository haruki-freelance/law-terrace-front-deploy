import { Text, View } from "react-native";
import { styles } from "./style";

/**
 * ヘッダー画面
 * 
 * @returns 
 */
export const Header = () => {
    return (
        <View style={styles.container}>
            <Text>HEADER</Text>
        </View>
    );
};